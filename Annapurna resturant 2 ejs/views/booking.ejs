<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Your Bookings - Annapurna</title>
  <link rel="stylesheet" href="/css/booking.css">
</head>
<body>
  <header>
    <nav>
      <div class="logo-container">
        <img src="/images/DRDO-logo-removebg-preview.png" alt="DRDO Logo" class="logo" />
        <div class="logo-text">Annapurna Restaurant</div>
      </div>
      <ul>
        <li><a href="/index"><b>Home</b></a></li>
        <li><a href="/menu">Menu</a></li>
        <li><a href="/booking">Bookings</a></li>
        <li><a href="/contact">Contact</a></li>
        <li><a href="/logout">Logout</a></li>
      </ul>
    </nav>
  </header>

  <div class="background-wrapper">
    <main class="booking-section">
      <h1>Your Bookings</h1>

      <% if (bookings.length === 0) { %>
        <p>No bookings found. Please make your first booking from the Menu page.</p>
      <% } else { %>
        <% bookings.forEach((booking, index) => { %>
          <div class="booking-card">
            <h3>Booking #<%= index + 1 %></h3>

            <p><strong>Employee ID:</strong> <%= booking.user.employeeId %></p>
            <p><strong>Name:</strong> <%= booking.user.name %></p>

            <ul>
              <% booking.items.forEach(item => { %>
                <li><%= item.name %> — ₹<%= item.price %></li>
              <% }) %>
            </ul>

            <p><strong>Total Amount:</strong> ₹<%= booking.total %></p>
            <p><strong>Date:</strong> 
              <%= new Date(booking.date).toLocaleDateString('en-IN', { 
                    day: '2-digit', month: 'short', year: 'numeric' 
              }) %>
            </p>
            <p><small>Booking ID: <%= booking._id %></small></p>
          </div>
        <% }) %>
      <% } %>
    </main>
  </div>

  <footer>
    <h2>📞 Contact Us</h2>
    <p>Email: abc@drdo.in | Phone: +91-12345-67890</p>
    <p><strong>About:</strong> Annapurna is a service initiative for quality food within the DRDO campus.</p>
    <p><strong>Booking Time:</strong> 8:00 AM – 10:30 AM daily</p>
  </footer>
</body>
</html>
